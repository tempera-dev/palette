/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.beater.client.kotlin.models

import ai.beater.client.kotlin.models.GateDecision
import ai.beater.client.kotlin.models.StatisticalTest

import com.squareup.moshi.Json
import com.squareup.moshi.JsonClass

/**
 * 
 *
 * @param adjustedAlpha 
 * @param baselineMean 
 * @param candidateMean 
 * @param ciHigh 
 * @param ciLow 
 * @param decision 
 * @param delta 
 * @param pValue Real two-sided p-value from `test`. The previous normal-approximation path reported no p-value at all.
 * @param sampleSize 
 * @param test 
 * @param mde Minimum detectable effect at the current sample size, in the metric's own units, at the gate's (adjusted) alpha and the standard power of 0.8 (§10.3 #5). Populated only when `decision` is `Inconclusive` — the comparison lacked the power to resolve the regression bound, and regressions smaller than this are invisible at this N. `None` on a conclusive decision (or when the paired differences have zero spread, so no effect-scale is defined). This replaces a bare \"underpowered\" flag with the actionable \"how small an effect could we even have seen\" number.
 * @param requiredN Number of paired observations that would be required to detect the *observed* effect at the gate's (adjusted) alpha and power 0.8 (§10.3 #5). Populated only when `decision` is `Inconclusive` and the observed effect is non-degenerate (non-zero delta over non-zero difference spread). `None` otherwise. This answers \"how many more cases would have made this conclusive?\".
 */


data class ExperimentComparison (

    @Json(name = "adjusted_alpha")
    val adjustedAlpha: kotlin.Double,

    @Json(name = "baseline_mean")
    val baselineMean: kotlin.Double,

    @Json(name = "candidate_mean")
    val candidateMean: kotlin.Double,

    @Json(name = "ci_high")
    val ciHigh: kotlin.Double,

    @Json(name = "ci_low")
    val ciLow: kotlin.Double,

    @Json(name = "decision")
    val decision: GateDecision,

    @Json(name = "delta")
    val delta: kotlin.Double,

    /* Real two-sided p-value from `test`. The previous normal-approximation path reported no p-value at all. */
    @Json(name = "p_value")
    val pValue: kotlin.Double,

    @Json(name = "sample_size")
    val sampleSize: kotlin.Int,

    @Json(name = "test")
    val test: StatisticalTest,

    /* Minimum detectable effect at the current sample size, in the metric's own units, at the gate's (adjusted) alpha and the standard power of 0.8 (§10.3 #5). Populated only when `decision` is `Inconclusive` — the comparison lacked the power to resolve the regression bound, and regressions smaller than this are invisible at this N. `None` on a conclusive decision (or when the paired differences have zero spread, so no effect-scale is defined). This replaces a bare \"underpowered\" flag with the actionable \"how small an effect could we even have seen\" number. */
    @Json(name = "mde")
    val mde: kotlin.Double? = null,

    /* Number of paired observations that would be required to detect the *observed* effect at the gate's (adjusted) alpha and power 0.8 (§10.3 #5). Populated only when `decision` is `Inconclusive` and the observed effect is non-degenerate (non-zero delta over non-zero difference spread). `None` otherwise. This answers \"how many more cases would have made this conclusive?\". */
    @Json(name = "required_n")
    val requiredN: kotlin.Int? = null

) {


}

