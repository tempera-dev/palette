/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package ai.beater.client.kotlin.models

import io.kotlintest.shouldBe
import io.kotlintest.specs.ShouldSpec

import ai.beater.client.kotlin.models.ExperimentComparison
import ai.beater.client.kotlin.models.GateDecision
import ai.beater.client.kotlin.models.StatisticalTest

class ExperimentComparisonTest : ShouldSpec() {
    init {
        // uncomment below to create an instance of ExperimentComparison
        //val modelInstance = ExperimentComparison()

        // to test the property `adjustedAlpha`
        should("test adjustedAlpha") {
            // uncomment below to test the property
            //modelInstance.adjustedAlpha shouldBe ("TODO")
        }

        // to test the property `baselineMean`
        should("test baselineMean") {
            // uncomment below to test the property
            //modelInstance.baselineMean shouldBe ("TODO")
        }

        // to test the property `candidateMean`
        should("test candidateMean") {
            // uncomment below to test the property
            //modelInstance.candidateMean shouldBe ("TODO")
        }

        // to test the property `ciHigh`
        should("test ciHigh") {
            // uncomment below to test the property
            //modelInstance.ciHigh shouldBe ("TODO")
        }

        // to test the property `ciLow`
        should("test ciLow") {
            // uncomment below to test the property
            //modelInstance.ciLow shouldBe ("TODO")
        }

        // to test the property `decision`
        should("test decision") {
            // uncomment below to test the property
            //modelInstance.decision shouldBe ("TODO")
        }

        // to test the property `delta`
        should("test delta") {
            // uncomment below to test the property
            //modelInstance.delta shouldBe ("TODO")
        }

        // to test the property `pValue` - Real two-sided p-value from `test`. The previous normal-approximation path reported no p-value at all.
        should("test pValue") {
            // uncomment below to test the property
            //modelInstance.pValue shouldBe ("TODO")
        }

        // to test the property `sampleSize`
        should("test sampleSize") {
            // uncomment below to test the property
            //modelInstance.sampleSize shouldBe ("TODO")
        }

        // to test the property `test`
        should("test test") {
            // uncomment below to test the property
            //modelInstance.test shouldBe ("TODO")
        }

        // to test the property `mde` - Minimum detectable effect at the current sample size, in the metric's own units, at the gate's (adjusted) alpha and the standard power of 0.8 (§10.3 #5). Populated only when `decision` is `Inconclusive` — the comparison lacked the power to resolve the regression bound, and regressions smaller than this are invisible at this N. `None` on a conclusive decision (or when the paired differences have zero spread, so no effect-scale is defined). This replaces a bare \"underpowered\" flag with the actionable \"how small an effect could we even have seen\" number.
        should("test mde") {
            // uncomment below to test the property
            //modelInstance.mde shouldBe ("TODO")
        }

        // to test the property `requiredN` - Number of paired observations that would be required to detect the *observed* effect at the gate's (adjusted) alpha and power 0.8 (§10.3 #5). Populated only when `decision` is `Inconclusive` and the observed effect is non-degenerate (non-zero delta over non-zero difference spread). `None` otherwise. This answers \"how many more cases would have made this conclusive?\".
        should("test requiredN") {
            // uncomment below to test the property
            //modelInstance.requiredN shouldBe ("TODO")
        }

    }
}
