/*
 * Beater API
 *
 * Agent observability, evaluation, gating, and human-review APIs for Beater
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// GateCandidateRequest : Request to gate a single optimization candidate (`gateOptimizationCandidate`).  The caller supplies the candidate it proposed and the per-case baseline-vs-candidate scores it observed, each tagged with its split. The server runs the held-out **Test** gate plus the anti-overfitting guardrail and returns the accept/reject verdict — the proposer never decides acceptance.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct GateCandidateRequest {
    /// The proposed change under evaluation (provenance for the audit trail).
    #[serde(rename = "candidate")]
    pub candidate: Box<models::GateCandidateChangeRequest>,
    /// Held-out Test gate policy. Defaults to the standard `GatePolicy`.
    #[serde(rename = "gate_policy", default, with = "::serde_with::rust::double_option", skip_serializing_if = "Option::is_none")]
    pub gate_policy: Option<Option<Box<models::GatePolicy>>>,
    /// Bootstrap confidence for the generalization-gap CI (default `0.95`).
    #[serde(rename = "overfit_confidence", default, with = "::serde_with::rust::double_option", skip_serializing_if = "Option::is_none")]
    pub overfit_confidence: Option<Option<f64>>,
    /// Bootstrap resamples for the generalization-gap CI (default `2000`).
    #[serde(rename = "overfit_resamples", default, with = "::serde_with::rust::double_option", skip_serializing_if = "Option::is_none")]
    pub overfit_resamples: Option<Option<i32>>,
    /// Seed for the deterministic generalization-gap bootstrap (default `1`).
    #[serde(rename = "overfit_seed", default, with = "::serde_with::rust::double_option", skip_serializing_if = "Option::is_none")]
    pub overfit_seed: Option<Option<i64>>,
    /// Largest benign generalization gap (default `0.0`).
    #[serde(rename = "overfit_tolerance", default, with = "::serde_with::rust::double_option", skip_serializing_if = "Option::is_none")]
    pub overfit_tolerance: Option<Option<f64>>,
    /// Per-case paired scores. Must include at least one `test` case and at least one `train`/`val` case so both the gate and the gap check are defined.
    #[serde(rename = "scores")]
    pub scores: Vec<models::GateCaseScoreRequest>,
}

impl GateCandidateRequest {
    /// Request to gate a single optimization candidate (`gateOptimizationCandidate`).  The caller supplies the candidate it proposed and the per-case baseline-vs-candidate scores it observed, each tagged with its split. The server runs the held-out **Test** gate plus the anti-overfitting guardrail and returns the accept/reject verdict — the proposer never decides acceptance.
    pub fn new(candidate: models::GateCandidateChangeRequest, scores: Vec<models::GateCaseScoreRequest>) -> GateCandidateRequest {
        GateCandidateRequest {
            candidate: Box::new(candidate),
            gate_policy: None,
            overfit_confidence: None,
            overfit_resamples: None,
            overfit_seed: None,
            overfit_tolerance: None,
            scores,
        }
    }
}

