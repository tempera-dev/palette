/*
 * Palette API
 *
 * Agent observability, evaluation, gating, and human-review APIs for Palette
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// PublicJudgeAuditRecord : Client-facing judge ledger row. The backing `provider`, the `provider_secret_id`, and our raw `provider_cost` are INTERNAL (staff-only) and must never reach a customer — exposing `provider_cost` alongside `charged_cost` would also leak our margin (billing-credits-contract §11). Only customer-facing fields appear here, including `charged_cost` (the amount the customer actually pays).
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct PublicJudgeAuditRecord {
    #[serde(rename = "cached")]
    pub cached: bool,
    #[serde(rename = "charged_cost")]
    pub charged_cost: Box<models::Money>,
    #[serde(rename = "created_at")]
    pub created_at: String,
    #[serde(rename = "evaluator_id")]
    pub evaluator_id: String,
    #[serde(rename = "judge_call_id")]
    pub judge_call_id: String,
    #[serde(rename = "model")]
    pub model: String,
    #[serde(rename = "project_id")]
    pub project_id: String,
    #[serde(rename = "request_hash")]
    pub request_hash: String,
    #[serde(rename = "response_hash")]
    pub response_hash: String,
    #[serde(rename = "score")]
    pub score: f64,
    #[serde(rename = "tenant_id")]
    pub tenant_id: String,
}

impl PublicJudgeAuditRecord {
    /// Client-facing judge ledger row. The backing `provider`, the `provider_secret_id`, and our raw `provider_cost` are INTERNAL (staff-only) and must never reach a customer — exposing `provider_cost` alongside `charged_cost` would also leak our margin (billing-credits-contract §11). Only customer-facing fields appear here, including `charged_cost` (the amount the customer actually pays).
    pub fn new(cached: bool, charged_cost: models::Money, created_at: String, evaluator_id: String, judge_call_id: String, model: String, project_id: String, request_hash: String, response_hash: String, score: f64, tenant_id: String) -> PublicJudgeAuditRecord {
        PublicJudgeAuditRecord {
            cached,
            charged_cost: Box::new(charged_cost),
            created_at,
            evaluator_id,
            judge_call_id,
            model,
            project_id,
            request_hash,
            response_hash,
            score,
            tenant_id,
        }
    }
}

