/*
 * Beater API
 *
 * Agent observability, evaluation, gating, and human-review APIs for Beater
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// Signature : A structural fingerprint of a trace's failure shape.  Two traces with the same ordered failing-span shingles share a [`Signature`] (and therefore the same [`Signature::hash`]). The `shingles` set is also used for Jaccard similarity during clustering.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct Signature {
    /// Stable sha256 hash of the ordered shingles.
    #[serde(rename = "hash")]
    pub hash: String,
    /// Ordered `(kind|status)` shingles of failing spans.
    #[serde(rename = "shingles")]
    pub shingles: Vec<String>,
}

impl Signature {
    /// A structural fingerprint of a trace's failure shape.  Two traces with the same ordered failing-span shingles share a [`Signature`] (and therefore the same [`Signature::hash`]). The `shingles` set is also used for Jaccard similarity during clustering.
    pub fn new(hash: String, shingles: Vec<String>) -> Signature {
        Signature {
            hash,
            shingles,
        }
    }
}

