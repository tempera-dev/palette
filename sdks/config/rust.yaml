# openapi-generator config: Rust control-plane client (Layer 1).
# Consumed by the Rust SDK and the palettectl CLI so all three share one contract.
generatorName: rust
additionalProperties:
  packageName: palette-client
  packageVersion: 0.1.0
  library: reqwest
  supportAsync: true
  useSingleRequestParameter: true
